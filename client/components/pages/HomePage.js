import React from 'react';

const styles = {
  container: {
    display: 'flex',
  }
};

const HomePage = (props) => (
  <div style={styles.container}>
    <h1>HomePage</h1>
  </div>
);


export default HomePage;
